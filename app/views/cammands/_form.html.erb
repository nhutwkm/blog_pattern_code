<%= form_for(@cammand) do |f| %>
  <% if @cammand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cammand.errors.count, "error") %> prohibited this cammand from being saved:</h2>

      <ul>
      <% @cammand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :id_user %><br>
    <%= f.text_field :id_user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
