<marquee onmouseover="this.stop()" onmouseout="this.start()" scrollamount="2" style = "color:blue"  direction="right">DANH SACH THANH VIEN FULL</marquee>

<table border="1" style="width:50%">
  <thead>
  
    <tr>
       <th>id</th>
      <th><th>name</th></th>
      <th><th>email</th></th>
      <th>role_id</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |person| %>
      <tr>
        <td><%= @id=person.id %></td>
        <td><td><%= person.name %></td></td>
        <td><td><%= person.email %></td></td>
        <td><%= person.role_id %></td>

        <td><%= link_to 'eidt', "/edit?id=#{@id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<br>
<%= will_paginate @user %>